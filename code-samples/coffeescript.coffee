# this is a comment
# TODO this is a todo

###
 a long comment
###

myFunction = (param, param) ->
  if a == true
    await param.doThing true
  1234

class MyClass
    constructor: ->
      @asdfasdf = 'a string' + 'a'
      return

MyClass::doSomething = ->
  (a, b) ->
    a + b

c.doSomething c.data
c = new MyClass
regex = /\<[a-zA-Z]+\>/g
console.log template
